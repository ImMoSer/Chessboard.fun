Концептуальное обсуждение и план действий
Ваше видение абсолютно реализуемо. Давайте разобьем его на логические этапы, как вы и предложили. Я немного доработал ваш план, чтобы минимизировать количество обращений к разработчику фронтенда и сгруппировать связанные изменения.

Этап 1: Обновление игровой логики и данных
Цель: Внедрить новые механики (endgame_skill, attack_skill, skill_value) в ядро приложения. Фронтенд на этом этапе не затрагивается, но мы готовим для него почву.

Шаг 1.1: Изменение структуры данных (Сущности/Entities)

AppUser (src/app-user/app-user.entity.ts): Добавляем два новых числовых поля для хранения скиллов пользователя:

endgame_skill (начальное значение: 10)

attack_skill (начальное значение: 25)

UserActivity (src/user-activity/user-activity.entity.ts): Добавляем новую числовую колонку:

skill_value (начальное значение: 0)

AttackPuzzle (src/puzzles/attack-puzzle.entity.ts): Как и договорились, добавляем:

bw_value: number

Шаг 1.2: Обновление логики подбора пазлов

PuzzlesService (src/puzzles/puzzles.service.ts): Модифицируем методы подбора пазлов в соответствии с вашей новой логикой:

findNextFinishHimPuzzle: Будет принимать global_rating и endgame_skill пользователя. Логика поиска обновится для использования диапазона endgame_skill ± 3 для поля bw_value. Добавим и запасной (fallback) сценарий.

findNextAttackPuzzle: Аналогично, будет использовать global_rating и attack_skill для поиска по диапазонам Rating и bw_value (attack_skill ± 5).

Шаг 1.3: Реализация расчета скиллов

DTOs (src/n8n-proxy/dto/index.dto.ts): Чтобы реализовать логику +1 за успех / -1 за поражение, нам нужно знать результат. Я предлагаю добавить поле success: boolean в UpdateFinishHimStatsDto и AttackRecordDto.

AppUserService (src/app-user/app-user.service.ts): Создадим новый метод, например, updateUserSkills(userId, puzzleType, wasSuccessful), который будет инкрементировать или декрементировать endgame_skill / attack_skill, соблюдая заданные вами рамки (min/max).

UserActivityService (src/user-activity/user-activity.service.ts): Создадим новый метод recordSkillValue(userId, gameMode, value), который будет записывать bw_value (или bw_value_total для tower) в колонку skill_value для сегодняшней активности.

Связующие сервисы: Обновим N8nProxyService и TacticalTrainerService, чтобы после решения пазла они вызывали новые методы для обновления скиллов и записи skill_value.

Результат Этапа 1: Бэкэнд будет работать по новым правилам, корректно подбирать пазлы и накапливать новую статистику. Изменения для фронтенда пока не видны, но вся основа заложена. Можно коммитить и переходить к следующему этапу.

===================================================
Что мы сделали (Итоги Этапа 1)
Мы полностью реализовали бэкенд-часть для новой системы скиллов.

Обновили базу данных:

В таблицу app_user добавлены поля endgame_skill и attack_skill для отслеживания навыков игрока.

В таблицу user_activity добавлена колонка skill_value для учета "очков мастерства" за решение задач.

В сущность AttackPuzzle добавлено недостающее поле bw_value.

Изменили игровую логику:

Подбор пазлов в режимах finishHim и attack теперь работает по-новому, учитывая тактический рейтинг и соответствующий скилл (endgame_skill или attack_skill).

Расчет скиллов реализован в AppUserService: +1 очко за успех, -1 за неудачу, с соблюдением заданных лимитов.

Запись очков (skill_value) реализована в UserActivityService при успешном решении пазла.

Адаптировали API и воркфлоу:

Обновили DTO, чтобы бэкенд мог получать от фронтенда результат решения (success: true/false) и другие необходимые данные.

Сохранили обратную совместимость с n8n: при успешном решении пазла в n8n по-прежнему уходит унифицированное событие saveRecord с временем решения.

Ключевой результат: Бэкенд готов. Он корректно подбирает задачи по новой системе, считает скиллы и очки мастерства, и при этом не ломает старые процессы.
===================================================
Этап 2: Новые API для отображения статистики
Цель: Предоставить фронтенду два новых эндпоинта для отображения Общего скилла и Стрика. Вот здесь мы готовим конкретное ТЗ для фронтенд-разработчика.

Шаг 2.1: API для Общего скилла

UserActivityService: Создадим метод getOverallSkill(userId), который будет делать SQL-запрос для подсчета суммы skill_value за последние 1, 7, 14, 21 и 30 дней.

UserActivityController: Создадим новый эндпоинт, например, GET /api/activity/personal/overall-skill, который будет защищен AuthGuard и возвращать данные из сервиса.

Шаг 2.2: API для Скилл стрика

UserActivityService: Это самая интересная задача. Создадим метод getSkillStreak(userId). Он будет использовать более сложный SQL-запрос (вероятно, с оконными функциями), чтобы:

Сгруппировать активность по дням.

Посчитать сумму skill_value за каждый день.

Определить, сколько подряд дней (считая от сегодня назад) пользователь набирал 100+ очков скилла.

UserActivityController: Создадим эндпоинт GET /api/activity/personal/skill-streak.

Результат Этапа 2: У нас есть два новых, готовых к использованию API. Мы можем передать их спецификацию (URL, формат ответа) команде фронтенда для реализации визуализации.

Этот план позволяет нам двигаться итеративно, обеспечивает тестируемость на каждом шаге и четко разделяет внутреннюю логику от её представления.